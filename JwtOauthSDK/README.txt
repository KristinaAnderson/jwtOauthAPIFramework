Installation instructions for the SDK are as follows:

Unzip the folder
upload the SRC folder and the composer.json file into a subdirectory on your server
run composer install from command line inside that folder

this will install the vendor folder and all dependencies.
Prerequisites:  PHP and composer already installed
